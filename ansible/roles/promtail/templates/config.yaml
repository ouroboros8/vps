server:
  http_listen_port: 0
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

client:
  url: {{ url }}

scrape_configs:
- job_name: system
  entry_parser: raw
  static_configs:
  - targets:
      - localhost
    labels:
      job: varlogs
      __path__: /var/log/*.log
- job_name: gogs
  entry_parser: raw
  static_configs:
  - targets:
      - localhost
    labels:
      job: gogs
      __path__: /var/log/gogs/*.log
